// ... (hozirgi JavaScript kodingiz) ...

window.addEventListener('wheel', function(event) {
    if (event.deltaY > 0) { // Pastga aylantirish
      // Keyingi bo'limga o'tish va animatsiyani ishga tushirish
    } else { // Yuqoriga aylantirish
      // Oldingi bo'limga o'tish va animatsiyani ishga tushirish
    }
  });
  